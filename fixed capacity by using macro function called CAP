#include <stdio.h>
#define CAP 40
void rajesh(int a[],int n);
void rajesh(int a[CAP], int n) {
for (int i = 0; i < n&&i<CAP; i++) {
a[i]=1;
printf("%d", a[i]);
}
}
int main(){
int a[CAP], n=130;
rajesh(a,n);
}
